{
  "name": "oauth-consent-diff",
  "version": "0.2.0",
  "private": true,
  "description": "A local Manifest V3 extension that compares OAuth consent scopes against trusted application baselines.",
  "scripts": {
    "build": "sh scripts/build.sh",
    "package": "sh scripts/package.sh",
    "test": "node --test tests/unit/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
